# CSS Battleship

Debug CSS specificity wars by finding conflicting selectors. Run: `node css-battleship.js [selector]` to see what's overriding your styles. Because sometimes you need to nuke specificity from orbit.